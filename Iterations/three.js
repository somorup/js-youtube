/*
//forin loop -  can be used for objects
const myObj = {
    Hero1:'Superman',
    Hero2:'Batman'
}

for (const key in myObj) {
    //console.log(key);
    console.log(myObj[key]);
}

//running for array
const myArr=["ok","bye","hello"]
for (const key in myArr) {
   console.log(key); //difference with forof loop is here if we print the iteratior is do not give the array value directly
   console.log(myArr[key]);
}*/

//will not run over Map, if tried it gives no ouput